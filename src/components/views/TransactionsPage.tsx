import { Box } from "@chakra-ui/react";
import { StocksList } from "../StocksList";

export const TransactionsPage = () => {
  return (
    <Box padding={"30px"}>
      <StocksList />
    </Box>
  );
};
